<h1>Users</h1>

<table class="chart">
  <thead>
    <tr>
      <th>Position</th>
      <th>User</th>
      <th>Plays</th>
    </tr>
  </thead>
  <tbody>
  <% @users.each_with_index do |user, index| %>
    <tr>
      <td><%= @users.offset + index + 1 %></td>
      <td><%= link_to_user user %></td>
      <td><%= build_chart(user, @users) %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<%= will_paginate @users %>
